# AGENTS.md

## Cursor Cloud specific instructions

This is an empty/placeholder Java-oriented repository (`cursor-env-2`). It contains only a `README.md` and a Java `.gitignore`—no application code, build files, or dependencies.

### Environment

- **Java**: OpenJDK 21 is pre-installed (`java`, `javac` available on PATH).
- **No build tool** (Maven/Gradle) is configured. If a build tool is added later, install it and update the VM update script accordingly.
- **No services** need to be started; there is no application to run yet.

### Development notes

- The `.gitignore` is set up for Java artifacts (`.class`, `.jar`, `.war`, etc.).
- When application code is added, update this section with lint/test/build/run instructions.
